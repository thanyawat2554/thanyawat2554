// This file contains JavaScript code for the website. It can be used to add interactivity and dynamic behavior to the webpage.

document.addEventListener('DOMContentLoaded', () => {
    const greetingElement = document.getElementById('greeting');
    if (greetingElement) {
        greetingElement.textContent = 'Welcome to My Static Website!';
    }

    // Add more interactivity here as needed
});